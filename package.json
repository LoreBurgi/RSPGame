{
  "name": "desafio-final",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.0-rc.0"
  },
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build --dist-dir build --public-url /RSPGame",
    "deploy": "gh-pages -d build",
    "carpetas": "ls"
  },
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
